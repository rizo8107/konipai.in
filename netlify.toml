[build]
  command = "npm run build"
  publish = "dist"

# Handle SPA routing by redirecting all paths to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variable configuration will be done in the Netlify UI
# or using environment variables in the Netlify dashboard 